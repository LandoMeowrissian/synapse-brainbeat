// import Instruments from '../Instruments/Instruments.js';

// const InstrumentSelector = ({ onSelectInstrument }) => {
//   return (
//     <select onChange={(e) =>
//       onSelectInstrument(e.target.value)}>
//       {Object.key(Instruments).map((instrumentKey, index) => (
//         <option key={index} value={instrumentKey}>
//           {instrumentKey}
//         </option>
//       ))}

//     </select>
//   )
// }

// export default InstrumentSelector;
